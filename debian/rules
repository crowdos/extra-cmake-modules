#!/usr/bin/make -f

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

%:
	dh $@ --builddirectory=build --sourcedirectory=extra-cmake-modules --parallel

override_dh_auto_test:
	# Nothing
